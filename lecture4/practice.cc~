#include <iostream>
using std::cout;
using std::endl;
using std::cin;


int main (){
  for (int i=5; 0<i; i--){
    cout << i << endl;}
  char a='a',d='d',
    unknown = 'a'+'d';
  int nunknown = unknown;
  cout << nunknown << endl;
  int x = 2./3.;
  cout << x << endl;
  int b=1;
  if (0<b<1)
  { cout <<"foo"<< endl;}   
return 0;
  }


//C-x C-w : write under new name

// to get out of here
// first save:

// C-x C-s
// then exit:
// C-x C-c
// if you're stuck 
// C-g
// C-k : kill meaning successive line delete
// C-y : yank back
// C-v : next screen
// Esc-v: previous screen
// C-l : reposition current line
// Esc > : end of file
// Esc < : beginning of file
